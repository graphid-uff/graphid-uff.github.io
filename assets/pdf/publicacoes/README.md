# PDFs das Publicações

Coloque aqui os arquivos PDF das publicações (um por publicação).

Depois de adicionar um PDF, aponte para ele no campo `pdf:` do arquivo correspondente em `_publicacoes/`, por exemplo:

```yaml
pdf: assets/pdf/publicacoes/nome-do-arquivo.pdf
```
